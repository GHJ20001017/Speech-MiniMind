# 00. 语音基础：从波形到频谱

这一章不使用 PyTorch，只使用 Python、NumPy 和 Matplotlib。目标不是马上训练模型，而是回答一个更基础的问题：**计算机到底如何表示和观察一段声音？**

语音不是“文字的另一种文件格式”。人说话时，声带和口腔使空气产生连续的压力变化；麦克风把压力变化转换成电信号；声卡再把电信号测量成一串数字。语音模型看到的，最终就是这串数字。

## 1. 从空气振动到数字

真实声音是连续信号，可以写成随时间变化的函数 `x(t)`。计算机不能保存无限多个时间点，因此每隔固定时间测量一次：

```text
x[0], x[1], x[2], ..., x[N-1]
```

这些离散的数称为**采样点（sample）**。如果每秒采样 16000 次，就称采样率为 16 kHz。第 `n` 个采样点对应的时间是 `t[n] = n / sample_rate`。

## 2. WAV 文件里有什么

WAV 是一种容器格式，常见的 PCM WAV 直接保存采样数字。最重要的三个参数是：

- **采样率**：每秒记录多少个采样点。示例音频是 24000 Hz。
- **位深**：每个采样点使用多少位保存。16-bit PCM 的整数范围是 `-32768` 到 `32767`。
- **通道数**：单声道为 1，立体声通常为 2。

16-bit PCM 文件中的整数会被转换成浮点数：

```text
x_float = x_int16 / 32768
```

这样数值大致落在 `[-1, 1]`，不同音频的数值范围更容易比较。脚本读取 WAV 后就在这里完成归一化。

### 为什么一读取就转换

这一步主要有四个原因：

1. **消除存储格式的影响**：同样的声音如果分别用 16-bit、24-bit 或浮点 WAV 保存，整数范围不同。转换后，后续代码都可以使用统一的浮点表示。
2. **避免整数运算问题**：音频后面要乘窗、做平方、FFT 和神经网络计算。浮点数更适合这些运算，也不会因为整数截断而损失小数部分。
3. **控制数值范围**：`[-1, 1]` 的量级适合 NumPy 和神经网络，能够减少数值过大导致的不稳定。
4. **统一接口**：从读取函数返回的就是模型可以继续处理的 `float32` 波形，后续模块不需要反复判断 WAV 的位深。

这里要区分两种常被混淆的操作：

- **PCM 标度转换（当前脚本做的）**：`int16 / 32768`。它只是在改变表示方式，不会改变不同音频之间的相对音量。
- **按文件峰值归一化（当前脚本没有做）**：例如把每个文件都除以自己的最大绝对值。它会让每个文件的峰值都变成 1，可能抹掉原始录音的音量差异。

做语音识别时，第二种操作有时可以提高数据一致性，但也可能放大背景噪声；是否使用要根据数据集和任务决定。因此基础读取阶段只做安全、可逆的 PCM 标度转换。

## 3. 采样率与 Nyquist 定理

采样率太低时，一个高频信号可能看起来像另一个低频信号，这种错误叫**混叠（aliasing）**。Nyquist 采样定理告诉我们：采样率为 `Fs` 时，能够无歧义表示的最高频率约为 `Fs / 2`。

```text
16 kHz → 最高约 8 kHz
24 kHz → 最高约 12 kHz
```

人类语音的重要信息大多在 8 kHz 以内，因此 16 kHz 常用于 ASR；更高采样率保留更多高频细节，但也带来更长的序列和更高的计算成本。

## 4. 波形：时域中的声音

把采样点按时间排列，就得到**波形（waveform）**。横轴是时间，纵轴是归一化振幅：

- 振幅正负表示压力变化的方向，不代表正负音量。
- 振幅绝对值较大通常意味着瞬时能量较高，但不等同于人耳感知的响度。
- 说话段通常有明显的振荡，静音段接近 0；背景噪声会让静音段也不完全平直。

## 5. RMS：观察能量大小

均方根（Root Mean Square）用一个数字描述一段信号的平均能量：

```text
RMS = sqrt(mean(x[n]^2))
```

平方消除了正负振幅抵消的问题，再开平方使结果回到原来的量纲。当前脚本计算整段音频的全局 RMS；真正的语音处理通常会在 20～30 ms 的短窗口内计算短时 RMS，用于静音检测或 VAD。

## 6. 过零率：观察振荡快慢

过零率（Zero-Crossing Rate, ZCR）表示相邻采样点的符号发生变化的比例：

```text
ZCR = sign_changes / (number_of_samples - 1)
```

频率较高、噪声较多的信号通常更容易穿过 0，因此 ZCR 往往更高；浊音和低频成分较强的片段通常更低。它不是现代 ASR 的主要特征，但适合作为第一个简单的时域指标。

## 7. 为什么不能直接“看”频率

波形回答的是：**某一个时刻，振幅是多少？** 但我们还想知道：**这段声音由哪些频率组成？** 频谱就是把随时间变化的振幅换一种坐标表示，展示各个频率成分的强弱。

## 8. FFT 前为什么要加窗

这里的“周期信号”不是说真实的语音一定会周期重复，而是 **DFT 对有限长度序列的数学解释**：它把这 `N` 个采样点看成一个周期的内容，并在左右两端不断复制：

```text
x[0], x[1], ..., x[N-1], x[0], x[1], ..., x[N-1], ...
```

因此，DFT 实际上会检查“如果把这段片段首尾相接并无限重复，它包含哪些频率”。如果 `x[N-1]` 和 `x[0]` 数值差别很大，重复处就会出现突然跳变。例如：

```text
片段结尾：  0.80, 0.85, 0.90
片段开头： -0.10,-0.05, 0.00
```

周期拼接后会从 `0.90` 瞬间跳到 `-0.10`。这种尖锐的跳变本身包含很多高频成分，所以频谱中会出现原信号并没有的额外频率，这叫**频谱泄漏（spectral leakage）**。它不是 FFT 算错了，而是 FFT 正确地分析了“带有边界跳变的周期延拓信号”。

为了减小这个人为的跳变，通常在 FFT 前乘一个两端接近 0 的窗函数。当前脚本使用 Hann 窗：

```text
w[n] = 0.5 - 0.5 * cos(2πn / (N - 1))
x_windowed[n] = x[n] * w[n]
```

Hann 窗在片段两端接近 0，中间接近 1。加窗后，周期拼接处的两端都接近 0，跳变明显减小。脚本第二幅图是窗函数本身，第三幅图是实际送入 FFT 的加窗波形。

窗函数也不是免费的：它会降低片段两端的信号能量，并使频谱峰变宽。因此窗函数是在“减少泄漏”和“保持频率分辨率”之间的折中；Hann 窗是语音处理中常用的默认选择。

## 9. FFT 到底输出了什么

对长度为 `N` 的波形做离散傅里叶变换（DFT），可以理解为拿许多不同频率的正弦波和余弦波去匹配输入信号。FFT（Fast Fourier Transform）是快速计算 DFT 的算法。

FFT 输出 `X[k]` 是复数：

```text
X[k] = real[k] + j * imag[k]
```

每个频率 bin 包含：

- **幅度**：该频率成分有多强，`|X[k]| = sqrt(real[k]^2 + imag[k]^2)`。
- **相位**：该频率成分在时间上的偏移，`angle(X[k])`。

这里的 **frequency bin** 可以翻译成“频率格子”或“频率槽位”。长度为 `N` 的 FFT 不会输出所有可能的连续频率，而是只在一组固定位置上测量信号。第 `k` 个格子对应：

```text
f[k] = k * sample_rate / N
```

相邻格子的间隔叫频率分辨率：

```text
Δf = sample_rate / N
```

例如，16 kHz 音频使用 25 ms 的窗口时，`N = 16000 × 0.025 = 400`，因此每个 bin 间隔是 `16000 / 400 = 40 Hz`。这意味着这一次 FFT 主要在 `0 Hz、40 Hz、80 Hz、...` 这些位置上报告幅度；它不是说 37 Hz 的声音不存在，而是说当前窗口长度不足以把 37 Hz 和附近频率清晰地区分开。

增大 `N` 会让 bin 更密、频率分辨率更高，但需要更长的观察片段；对变化很快的语音来说，窗口太长又会损失时间分辨率。这就是时频分析中的基本折中。

当前脚本绘制的是 `abs(np.fft.rfft(...))`，因此看到的是幅度，没有绘制相位。第 `k` 个频率 bin 对应 `k * sample_rate / N`，频率分辨率是 `sample_rate / N`。

### 为什么纵轴是 dB

FFT 后最原始的幅度确实是线性的：

```python
spectrum = abs(FFT(x))
```

它表示每个频率 bin 的相对幅度。当前脚本为了绘图使用：

```python
magnitude_db = 20 * log10(max(spectrum, 1e-8))
```

因此图上的纵轴是 **幅度的分贝表示**，不是把“频率强度”换成了完全不同的东西。dB 只是用对数刻度表达强弱：

```text
幅度 dB：20 × log10(幅度 / 参考幅度)
功率 dB：10 × log10(功率 / 参考功率)
```

因为功率与幅度平方成正比，所以两种写法表达的是同一个相对强弱关系。当前代码的参考幅度隐含为 1.0，因此纵轴标成 `dB re 1.0` 更准确。

使用 dB 有两个实际原因：

1. 语音的强弱范围很大，线性坐标下强峰会把弱小但有用的频率细节压扁。
2. 人耳对相对变化更敏感，对数刻度更接近我们描述声音强弱的习惯。

如果想看未经对数压缩的“原始强度”，可以直接绘制 `spectrum`；如果想看物理意义更接近能量的量，可以绘制功率谱 `abs(FFT(x)) ** 2`。后续的 log-Mel 频谱同样是在功率或能量基础上取对数。

## 10. 为什么只画一半频率

输入波形是实数时，FFT 的负频率部分与正频率部分互为共轭，包含重复信息。因此脚本使用 `rfft`，只保留从 0 Hz 到 `sample_rate / 2` 的部分。示例音频采样率为 24 kHz，所以 Nyquist 频率是 12 kHz；绘图默认将显示范围限制在 8 kHz 以内，以聚焦语音。

以 `N = 600` 为例，完整的复数 FFT 会产生 600 个编号：

```text
k = 0, 1, 2, ..., 299, 300, 301, ..., 599
```

其中：

- `k = 0` 是 0 Hz，也叫 DC 分量；
- `k = 1` 到 `k = 299` 是正频率；
- `k = 300` 是 Nyquist 频率，也就是 `sample_rate / 2`；
- `k = 301` 到 `k = 599` 是负频率，对实数波形来说分别与前面的正频率重复。

因此 `rfft` 只需要保留：

```text
k = 0, 1, 2, ..., 299, 300
```

数量就是：

更直接的写法是：保留从 `k=0` 到 `k=N/2`，首尾都包含，所以数量为 `N/2 - 0 + 1 = N/2 + 1`。这里的“加一”不是额外制造了一个频率，而是因为 `k=0` 和 `k=N/2` 两个端点都要计数。对于奇数长度，公式是 `floor(N/2) + 1`。

## 11. 从采样点到频谱：完整中间产物

运行：

```bash
python scripts/analyze_audio.py examples/disgusted_to_happy.wav --plot outputs/example.png
```

为了避免把 12 秒长音频和窗函数画在同一尺度上，图表将“整体”和“局部”分开。默认放大第一个 25 ms 窗口；可以用 `--frame-ms` 修改窗口长度：

```bash
python scripts/analyze_audio.py examples/disgusted_to_happy.wav \
  --frame-ms 25 --plot outputs/example.png
```

图中四个面板依次展示：

```text
PCM 整数采样点
    ↓ 除以 32768
归一化波形 x[n]
    ↓ 乘以 Hann 窗
加窗波形 x[n] · w[n]
    ↓ FFT
复数频域系数 X[k]
    ↓ 取绝对值
幅度频谱 |X[k]|
```

1. **整体波形**：先看到完整音频中哪里有语音、哪里接近静音。
2. **25 ms 局部波形和 Hann 窗**：把同一时间轴上的原始采样值与窗权重放在一起比较。
3. **加窗后的采样点**：散点让初学者看到 FFT 输入确实是一组离散数字，而不是连续曲线。
4. **单帧频谱**：标题直接显示 bin 间距，帮助理解窗口长度如何决定频率分辨率。

终端还会打印采样率、采样点数量、时长、峰值振幅、RMS、过零率、FFT bin 数和频率分辨率。

## 12. 频谱与时频谱的区别

当前脚本只对整段音频做一次 FFT，因此得到的是一维总体频谱。它可以告诉我们“整段音频总体上有哪些频率”，但无法告诉我们“某个频率在第几秒出现”。

语音是随时间变化的，而且整句话通常并不是一个稳定的信号。元音、辅音、停顿会不断切换。因此我们把长音频切成许多较短的片段，在每个片段内近似认为信号的频率结构不会变化太快，这叫**短时平稳性（short-time stationarity）**。

这就是滑动窗口的来源。窗口有三个常见参数：

- **窗长（frame length）**：每个片段包含多少采样点，例如 25 ms。
- **帧移（hop length）**：窗口每次向前移动多少采样点，例如 10 ms。
- **重叠（overlap）**：相邻窗口重叠的部分。25 ms 窗长、10 ms 帧移意味着 15 ms 重叠。

窗口通常会重叠，因为如果每一帧完全不重叠，两个窗口交界处的信息会被切断；重叠可以让时间变化更连续，也降低边界对结果的影响。

你的直觉也解释了为什么不能只分析一个加窗片段：窗函数会把片段开头和结尾的振幅压低，这些位置的信息并没有消失，而是会在相邻窗口中再次出现，并且在相邻窗口的中间位置获得更大的权重。可以把它想成给同一段声音拍很多张有重叠的照片：某个窗口里处在边缘的细节，在另一张照片里可能正好处在中心。

在分析阶段，这种重叠让每个时间点通常被多个窗口观察到；在需要从频谱重建波形时，还可以使用 **overlap-add（重叠相加）**，并除以各个窗函数权重的总和，补偿不同位置的衰减。满足特定条件的窗长和帧移（例如合适设置下的 Hann 窗）可以让这些权重之和近似常数，这称为 constant-overlap-add（COLA）条件。

在每个窗口内，我们仍然会乘 Hann 窗。它的作用不是“保证窗口内的信号是周期的”，而是让这个窗口在进行周期延拓时，首尾更平滑：

```text
长语音
  → 取第 1 个短窗口 → Hann 窗 → FFT
  → 向右移动 hop   → Hann 窗 → FFT
  → 向右移动 hop   → Hann 窗 → FFT
  → 把每帧频谱按时间排列
```

所以需要区分两件事：

1. **滑动窗口**：把变化很快的长语音变成一系列短时、近似稳定的分析片段。
2. **窗函数**：减少每个片段在周期延拓时的边界跳变和频谱泄漏。

语音信号本身仍然不一定周期，窗函数也不能让它变成真正的周期信号。重叠窗口也不是让每一个单独片段都“完整无损”，而是让同一信息被邻近窗口重复覆盖，从整体上减少边缘衰减造成的影响。

短时傅里叶变换（STFT）的过程是：

```text
波形 → 切成许多短帧 → 每帧加窗 → 每帧 FFT → 按时间排列 → 二维时频谱
```

常见语音模型还会继续计算功率、通过 Mel 滤波器组并取对数：

```text
STFT → |X|² → Mel filter bank → log-Mel spectrogram
```

这就是下一章要实现的内容。届时特征会从一维波形变成类似“图片”的二维张量，随后才能交给 Conformer 或 Transformer 声学编码器。

## 13. 动手实验

建议每次只改变一个因素：

1. 把音频音量乘以 0.5，观察峰值和 RMS 如何变化。
2. 截取一段静音和一段语音，比较它们的波形、RMS 和频谱。
3. 将采样率改成 16 kHz，观察 Nyquist 频率和频谱范围的变化。
4. 暂时去掉 Hann 窗，对比加窗前后的频谱泄漏。
5. 对一段短纯音运行脚本，观察频谱峰值是否出现在预期频率附近。

## 14. 第一个 STFT 时频图

运行：

```bash
python scripts/analyze_audio.py examples/disgusted_to_happy.wav \
  --stft-plot outputs/stft.png
```

脚本默认使用 25 ms 窗长和 10 ms 帧移。对每一个窗口，它都会重复执行：

```text
取出一帧 → 乘 Hann 窗 → rFFT → 取幅度
```

然后把所有帧的幅度频谱按照时间堆叠起来。结果是一张二维图：

- 横轴：窗口中心的时间
- 纵轴：频率
- 颜色：该时间和频率上的幅度，颜色越亮表示能量越强

这张图比整段音频的一维频谱更适合语音，因为它能显示元音、辅音和停顿在不同时间出现的频率变化。终端会打印类似 `stft_shape: (1274, 301)` 的信息，分别表示帧数和每帧的频率 bin 数。

改变 `--hop-ms` 可以观察时间采样密度的变化；改变 `--frame-ms` 可以观察频率分辨率和时间分辨率之间的折中。下一步会在 STFT 之上实现 Mel 滤波器组和 log-Mel 频谱。

如果只看静态图，仍然看不到“窗口是如何移动的”。可以生成一个教学动画：

```bash
python scripts/analyze_audio.py examples/disgusted_to_happy.wav \
  --stft-gif outputs/stft_process.gif
```

动画中，橙色和红色边界沿着完整波形向右移动；每移动一次，就对当前 25 ms 片段加窗并计算一次 FFT；下方时频图随之增加一列。为了控制文件大小，脚本默认从全部窗口中均匀抽取最多 120 帧用于 GIF 展示，数值计算仍然覆盖整段音频。
